[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && npx tsx src/main.ts"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
