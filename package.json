{
  "name": "sanad-protocol",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "install": "cd backend && pnpm install",
    "build": "cd backend && pnpm run migrate || echo 'Migrations will run on startup'",
    "start": "cd backend && npx tsx src/main.ts"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
